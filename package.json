{
  "name": "podcast-scraper",
  "version": "1.0.0",
  "description": "A simple Node.js app to scrape podcasts from RSS feeds",
  "main": "server/index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "client": "cd client && npm start",
    "build": "cd client && npm install && npm run build",
    "netlify-build": "rm -rf node_modules client/node_modules && cd client && npm ci --production=false && npm run build"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "axios": "^1.6.2",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.2",
    "rss-parser": "^3.13.0",
    "spotify-web-api-node": "^5.0.2",
    "swagger-jsdoc": "^6.2.8",
    "swagger-ui-express": "^5.0.1",
    "ws": "^8.18.1"
  },
  "devDependencies": {
    "netlify-cli": "^23.5.1",
    "nodemon": "^3.1.10",
    "puppeteer": "^24.13.0"
  }
}
